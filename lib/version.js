/*
 * The release version shown in the GUI. Keep each component in the range
 * 0-99; the UI uses the conventional unpadded form (for example, 1.0.1).
 */
const APP_VERSION = '1.0.4';

module.exports = { APP_VERSION };
